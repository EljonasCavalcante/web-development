/* function declation ( essa função tem vantagem de se chamdada antes sem dá erro*/
function soma (x, y) {
  return x +y
}

// function expression
const sub = function (x, y) { 
  return x - y  
}

// named function expresssion

const mult = function mult(x, y) {
  return x * y
}