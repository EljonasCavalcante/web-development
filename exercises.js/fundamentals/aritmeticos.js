// operadores binário

const [a, b, c, d] = [3, 5, 1, 15] 

const soma = a + b + c + d
const sutracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 // mostra o resta da divisão.

console.log(soma, sutracao, multiplicacao, divisao, modulo)
